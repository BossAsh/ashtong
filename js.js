myFunction(){
	var txt;
  	var token = prompt("Please enter your token for placing Orders!");
  if (token ==  "bakaashtongyan") {
    window.open(place.html);
  } else {
    txt = "Wrong Token";
  }
}